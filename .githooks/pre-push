#!/bin/sh
# Block pushes directly to main
while read local_ref local_sha remote_ref remote_sha
do
  case "$local_ref" in
    refs/heads/main)
      echo "Direct pushes to main are blocked. Push to develop and open a PR." 1>&2
      exit 1
      ;;
  esac
done
exit 0